-- book.mk4 — make4ht build file
-- Minimal: just enable dvisvgm_hashes for TikZ SVG caching.
-- The build sequence (htlatex/biber passes) is controlled by make4ht defaults.

Make:enable_extension("dvisvgm_hashes")
