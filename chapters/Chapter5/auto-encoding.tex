\begin{quote}
\hfill  ``{\em It has been obvious since the 1980s that backpropagation through deep autoencoders would be very effective for nonlinear dimensionality reduction, provided that computers were fast enough, data sets were big enough, and the initial weights were close enough to a good solution. All three conditions are now satisfied}.''

$~$\hfill -- Geoffrey Hinton and Russ  Salakhutdinov, 2006
\end{quote}




Autoencoding to ensure correctness and consistency: from PCA, dictionary learning to more general low-dimensional distributions, say mixture of low-dim subspaces and manifolds. Also from classical autoencoding with two separate, mutually inverse, mappings, to closed loops such as diffusion/denoising and sparse coding/decoding. 

\section{Consistent Representations}
Definition of consistent representations, and their relations to autoencoding (which has a long history of study).


\begin{definition}[Bidirectional Representation Learning]\label{def:bidirectional_rep}
    Given data \(\vX\), an \textit{consistent representation} is a pair of functions \((f \colon \cX \to \cZ, g \colon \cZ \to \cX)\), such that the \textit{features} \(\vZ = f(\vX)\) are compact and structured, and the \textit{autoencoding} \(\wh{\vX} \doteq g(\vZ)\) is \textit{close} to \(\vX\). We say that the representation is \textit{distributionally consistent} if \(\Law(\vX) \approx \Law(\wh{\vX})\); we say that it is \textit{sample-wise} consistent if \(\vX \approx \wh{\vX}\) with high probability.
\end{definition}


\section{From PCA to Nonlinear PCA}
Generalize PCA to nonlinear PCA,  autoencoding and autoassociative networks.

How to ensure the representation is compact and structured, say linear or piecewise linear, or informative? Classical autoencoders do not explicitly impose such... 

Incremental flattening transformation, particularly  \href{https://arxiv.org/abs/2305.01777}{nonlinear manifold flattening and reconstruction}. 

\section{From Matrix Completion to Masked Autoencoding}
Generalize low-rank matrix completion to masked autoencoding (MAE).

Examples on real imagery data: \href{https://arxiv.org/abs/1012.3216}{low-rank texture completion (TILT)} to \href{https://arxiv.org/abs/2404.02446}{masked image completion}.

\section{Autoencoding via Denoising and Diffusion}
A more general autoencoding for compact and structured representations via diffusion and denoising. (a better DiT version with a CRATE backbone.) Maybe trying to optimize rate reduction type objective... 

Theoretical justification: maybe mention the recent work from MAE and Qing Qu's group about memorization and generalization (diffusion process for the PCA or mixture of low-dim Gaussians case).

\section{Summary}